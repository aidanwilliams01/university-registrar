@{
  Layout = "_Layout";
}

@model Registrar.Models.Student
@* <p><strong>NOTE:</strong> You need to have at least one category before you can add an student!</p>
<p>Go to @Html.ActionLink("this page", "Create", "Categories") to create a category.</p>
<h4>Add a new student</h4> *@
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <br>
  @* @Html.LabelFor(model => model.Category)
  @Html.DropDownList("CategoryId")
  <br> *@
  @Html.LabelFor(model => model.EnrollmentDate)
  @Html.TextBoxFor(model => model.EnrollmentDate, new { type = "date" })
  <br>
  @* @Html.LabelFor(model => model.StudentCompletion, "Student Completed?")
  @Html.CheckBoxFor(model => model.StudentCompletion)
  <br> *@
  <input type="submit" value="Add new student"/>
}
<p>@Html.ActionLink("Show all students", "Index")</p>